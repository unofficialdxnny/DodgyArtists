// set local storage values

localStorage.setItem("lastPage", "index")


// Video gallery

// play song

// const buttonElement = document.querySelector('button[data-testid="play-pause-button"]');

// // Check if the button element exists
// if (buttonElement) {
//   // Delay for 3 seconds and then trigger a click event on the button element
//   setTimeout(() => {
//     buttonElement.click();
//   }, 3000);
// } else {
//   console.log("Button element not found.");
// }
